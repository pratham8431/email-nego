services:
  - type: web
    name: influencer-negotiation-api
    plan: free  # Explicit free tier
    dockerfilePath: Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        value: your_actual_api_key_here
      - key: PORT
        value: 8000
    healthCheckPath: /health
    autoIdle: true  # Saves free tier hours
